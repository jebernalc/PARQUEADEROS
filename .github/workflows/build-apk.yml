name: Build PARKOPS Licensed APK

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - apk-baseline-license-control-2026-09-06

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Reconstruct recovered licensed baseline
        shell: bash
        run: |
          mkdir -p app/src/main/assets
          cat source/baseline/index.html.gz.b64.* | base64 --decode | gzip -dc > app/src/main/assets/index.html
          echo "4a4e077b2b0e730191af902b63fa626fb10d4d1836cd40ef66b00e1d5df76494  app/src/main/assets/index.html" | sha256sum -c -
          test "$(wc -c < app/src/main/assets/index.html)" -eq 66599

      - name: Validate licensed baseline
        shell: bash
        run: |
          grep -q "Activación PARKOPS" app/src/main/assets/index.html
          ! grep -qiE 'QR Nequi|id="celular"|id="mCel"|3114980095' app/src/main/assets/index.html
          grep -q "activateLicense" app/src/main/java/com/solvex/parkops/MainActivity.java
          grep -q "ANDROID_ID" app/src/main/java/com/solvex/parkops/MainActivity.java

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'

      - name: Build installable Android APK
        run: gradle assembleDebug --no-daemon

      - name: Verify compiled APK internals
        shell: bash
        run: |
          APK=app/build/outputs/apk/debug/app-debug.apk
          test -f "$APK"
          unzip -p "$APK" assets/index.html | sha256sum | grep -q '^4a4e077b2b0e730191af902b63fa626fb10d4d1836cd40ef66b00e1d5df76494'
          unzip -l "$APK" | grep -q 'AndroidManifest.xml'
          FOUND=0
          while read -r dex; do
            if unzip -p "$APK" "$dex" | strings | grep -q 'Lcom/solvex/parkops/MainActivity;'; then FOUND=1; break; fi
          done < <(unzip -Z1 "$APK" | grep -E '^classes[0-9]*\.dex$')
          test "$FOUND" -eq 1

      - name: Package release files
        shell: bash
        run: |
          mkdir -p release
          cp app/build/outputs/apk/debug/app-debug.apk release/PARKOPS-SOLVEX-LICENSED-v1.1.0.apk
          cp tools/license_generator.py release/license_generator.py
          cp keys/solvex_license_public.pem release/solvex_license_public.pem
          cp docs/LICENSE-CONTROL.md release/LICENSE-CONTROL.md
          sha256sum release/PARKOPS-SOLVEX-LICENSED-v1.1.0.apk > release/PARKOPS-SOLVEX-LICENSED-v1.1.0.sha256

      - uses: actions/upload-artifact@v4
        with:
          name: PARKOPS-SOLVEX-LICENSED-v1.1.0
          path: release/
          if-no-files-found: error
